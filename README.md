# Библиотечный каталог

## Учётные данные

- **Библиотекарь**: логин `librarian1`, пароль `TestPass123!`
- **Читатель**: логин `guest1`, пароль `TestPass123!`

## Технические детали

- Проект использует Django 5, PostgreSQL (в контейнере Docker), Bootstrap 5, htmx.
- База данных автоматически наполняется из фикстуры `catalog/fixtures/initial_data.json`.
